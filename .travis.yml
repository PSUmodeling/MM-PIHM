language: c
compiler: gcc
script:
  - cmake --version
  - make cvode && make pihm
  - make clean && make flux-pihm
  - make clean && make flux-pihm-bgc
  - make clean && make OPENMP=on pihm
  - make clean && make OPENMP=on flux-pihm
  - make clean && make OPENMP=on flux-pihm-bgc
branches:
  only:
  - master
